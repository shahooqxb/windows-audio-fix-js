# 🔊 Windows Audio Fix (JavaScript + PowerShell)

Fix **missing, deleted, or broken sound drivers** on **Windows 11**
using **JavaScript (Node.js)** that runs **PowerShell / CMD commands**.

✔ No third-party tools  
✔ Uses built-in Windows commands  
✔ Works with Realtek / Intel / Generic audio  

---

## ⚠ REQUIREMENTS
- Windows 11
- Node.js installed
- Run terminal **AS ADMINISTRATOR**

---

## 🚀 HOW TO USE

1. Clone this repository or download ZIP
2. Open **Command Prompt or PowerShell as Administrator**
3. Run:

```cmd
node fix-audio.js
